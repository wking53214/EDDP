# _unsorted

Content that landed in EDDP but isn't confirmed EDDP-specific and has no
clear destination yet elsewhere in the sweep.

- `ws3_telemetry_guardrail_source.py` / `_adapter.py`: a "Deadman Switch"
  telemetry guardrail. No confirmed overlap found anywhere else in the
  repo sweep so far. Needs William's direct decision on where this
  belongs — not moved automatically.
